rule all:
    input:
        's1_file.txt',
        's1_dir' # `directory()` not needed

rule touch:
    output:
        's1_file.txt'
    shell:
        'touch {output}'

rule mkdir:
    output:
        directory('s1_dir')
    shell:
        'mkdir -p {output}'